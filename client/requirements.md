## Packages
framer-motion | Page transitions and smooth interactive elements

## Notes
WebSocket connects to /ws path
The backend must broadcast WS_EVENTS.UPDATE_CHARACTER when any character is updated
